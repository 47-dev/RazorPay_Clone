{
    "builds": [
      {
        "src": "index.html",
        "use": "@vercel/node",
        "config": { "maxLambdaSize": "2048mb" },
        "command": "npm install && npm run build"
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/index.html" }
    ]
  }
  